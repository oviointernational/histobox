kind = "web"
previewPath = "/"
title = "Histobox"
version = "1.0.0"
id = "artifacts/histobox"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20154

[services.development]
run = "pnpm --filter @workspace/histobox run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/histobox", "run", "build" ]
publicDir = "artifacts/histobox/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20154"
BASE_PATH = "/"
